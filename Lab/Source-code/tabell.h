/* tabell.h */
/* Developer MissPink */

#include "ranta.h"
#include <stdio.h>

#if !defined(tabell_h)
	#define tabell_h

   #define	ABS( x )	( (x) > 0 ? (x) : -(x) )
	void TabellPaSkarmen( double kapital, int antalAr );

#endif